export interface OwnerForCreation {
    name: string;
    dateOfBirth: string;
    address: string;
}
export interface OwnerForUpdate {
    name: string;
    dateOfBirth: string;
    address: string;
    }